# Supabase Configuration
VITE_SUPABASE_URL=votre_supabase_url
VITE_SUPABASE_ANON_KEY=votre_supabase_anon_key

# Instructions:
# 1. Créez un projet sur https://supabase.com
# 2. Copiez l'URL et la clé anon depuis les paramètres du projet
# 3. Renommez ce fichier en .env.local
# 4. Remplacez les valeurs par vos clés Supabase

# Configuration de la base de données:
# Table publique: profiles
# Colonnes requises:
# - id (UUID, primary key, référence vers auth.users.id)
# - email (text)
# - first_name (text)
# - last_name (text)
# - role (text: 'candidate', 'recruiter', 'admin')
# - avatar_url (text, nullable)
# - created_at (timestamp with time zone, default: now())
# - updated_at (timestamp with time zone, default: now())

# SQL pour créer la table profiles:
/*
CREATE TABLE public.profiles (
  id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  email TEXT NOT NULL,
  first_name TEXT NOT NULL,
  last_name TEXT NOT NULL,
  role TEXT NOT NULL CHECK (role IN ('candidate', 'recruiter', 'admin')) DEFAULT 'candidate',
  avatar_url TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  PRIMARY KEY (id)
);

-- Activer RLS (Row Level Security)
ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;

-- Politique pour permettre aux utilisateurs de voir leur propre profil
CREATE POLICY "Users can view own profile" ON public.profiles
  FOR SELECT USING (auth.uid() = id);

-- Politique pour permettre aux utilisateurs de mettre à jour leur propre profil
CREATE POLICY "Users can update own profile" ON public.profiles
  FOR UPDATE USING (auth.uid() = id);

-- Politique pour permettre aux utilisateurs de créer leur profil lors de l'inscription
CREATE POLICY "Users can insert own profile" ON public.profiles
  FOR INSERT WITH CHECK (auth.uid() = id);

-- Politique pour permettre aux admins de voir tous les profils
CREATE POLICY "Admins can view all profiles" ON public.profiles
  FOR SELECT USING (
    EXISTS (
      SELECT 1 FROM public.profiles
      WHERE id = auth.uid() AND role = 'admin'
    )
  );
*/